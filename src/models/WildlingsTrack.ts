export class WildlingsTrack {
    id = null 
    wildlingsStrength = null 
    
    constructor({id, wildlingsStrength}) {
        this.id = id
        this.wildlingsStrength = wildlingsStrength
        
    }
}